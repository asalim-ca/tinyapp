<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
    </div>
    <% if ( user && user.id ) { %>
      <form method="POST" action="/logout" class="d-flex">
        <label class="form-control-plaintext px-2">Logged In As: <%= user.email %></label>
        <input type="submit" value="Logout" class="btn btn-outline-dark">
      </form>
    <% } else { %>
      <form method="GET" action="/login" class="d-flex">
        <a href="/register" class="nav-item nav-link text-warning">Register</a>
        <button type="submit" class="btn btn-dark mb-2">Login</button>
      </form>
  <% } %>
    
  </nav>
</header>